# PRD 反哺：遗漏场景补充清单

| 项目 | 内容 |
|------|------|
| 关联 PRD | `prd/新设备初始化/web-device-init-prd.md` v0.4 |
| 关联策略 | `DeviceInit_V190_pc_Strategy.md` |
| 平台 | PC（Electron + Web） |
| 生成日期 | 2026-04-08 |
| 扫描维度 | 9（含 UI/交互一致性） |
| 补充条数 | 24 |

> 以下内容由测试策略 9 维度扫描自动生成。请逐条确认或标注"不适用"。确认后的条目将纳入用例设计基线。

---

## 1. 状态机路径遗漏 `[策略补充]`

| # | 遗漏场景 | 关联 FR | 说明 |
|---|---------|--------|------|
| S1 | 第二步注册接口调用中途网络断开 | FR-2.4 | PRD 仅提到"调用失败→提示重试"，未说明：已提交的数据是否回滚？重试是否需要重新填写？ |
| S2 | 云账号绑定后用户在第三步点击"跳过"而非"完成" | FR-3.8 | PRD 说"跳过：不绑定、不勾选"，但若已绑定成功后再点跳过，绑定状态是否保留？ |
| S3 | 步骤 1→2→3 过程中浏览器刷新（Web 端）/ 应用崩溃重启（Electron） | 全流程 | PRD 未定义中途刷新行为：是回到第一步？还是恢复到当前步骤？ |
| S4 | 已初始化设备被恢复出厂后再次初始化 | FR-0.4 | PRD 提到"恢复出厂后重新初始化"是目标用户，但未说明此时设备名/时区是否有残留默认值 |

## 2. 异常场景遗漏 `[策略补充]`

| # | 遗漏场景 | 关联 FR | 说明 |
|---|---------|--------|------|
| E1 | 设备名输入框粘贴超长文本（>1000字符） | FR-1.2 | 仅定义"最多 25 字符"，未说明超长粘贴的截断还是拒绝行为 |
| E2 | 密码输入含 emoji / Unicode 特殊字符 | FR-2.2 | "不允许包含空格"，但未明确是否允许其他非可打印字符 |
| E3 | 云账号绑定页面打开后长时间不操作（>30min），回到 NAS 页面点完成 | FR-3.3 | 绑定回调是否有超时机制？ |
| E4 | 多个浏览器标签同时访问同一未初始化设备（Web 端） | FR-0.5 | 并发注册冲突如何处理？ |

## 3. 边界条件遗漏 `[策略补充]`

| # | 遗漏场景 | 关联 FR | 说明 |
|---|---------|--------|------|
| B1 | 设备名恰好 25 字符 vs 26 字符 | FR-1.2 | 边界值需明确 |
| B2 | 用户名恰好 25 字节（含多字节中文：如 8 个中文+1 字母=25 字节） | FR-2.1 | "最长 25 字节（支持多字节字符）" 与 FR-1.2 的"25 字符"单位不同，容易混淆 |
| B3 | 密码恰好 8 字符 / 24 字符 | FR-2.2 | 边界值 |
| B4 | 设备名仅含连字符/下划线（如 "---" 或 "___"） | FR-1.2 | 合法但异常的输入 |

## 4. 权限组合遗漏 `[策略补充]`

| # | 遗漏场景 | 说明 |
|---|---------|------|
| P1 | 首次初始化创建的管理员与后续普通用户的权限差异 | PRD 未说明管理员创建后是否自动登录 |

## 5. 平台差异遗漏 `[策略补充]`

| # | 遗漏场景 | 关联 FR | 说明 |
|---|---------|--------|------|
| D1 | Web 端直接输入 IP 访问但 IP 错误/设备离线 | FR-0.5 | PRD 仅说"自动检测设备初始化状态"，未定义连接失败的错误提示 |
| D2 | Electron 端 UDP 扫描在 VPN/双网卡环境下发现多个网段设备 | FR-0.1 | 扫描范围限制未定义 |
| D3 | Web 端使用 IE / 旧版浏览器访问 | NFR | 最低浏览器版本要求未定义 |

## 6. 并发场景遗漏 `[策略补充]`

| # | 遗漏场景 | 说明 |
|---|---------|------|
| C1 | Electron + Web 同时对同一设备发起初始化 | 先完成的注册成功，后到的应如何表现？ |
| C2 | 同一 Electron 客户端快速多次点击"下一步" | 防重复提交机制 |

## 7. 数据完整性遗漏 `[策略补充]`

| # | 遗漏场景 | 说明 |
|---|---------|------|
| I1 | 注册成功后设备名、时区、管理员信息是否正确持久化 | 初始化完成后进系统，设置中是否能看到刚才设置的设备名和时区 |
| I2 | 云账号绑定状态是否持久化到设备 | 重启 NAS 后绑定状态是否保留 |

## 8. 降级兜底遗漏 `[策略补充]`

| # | 遗漏场景 | 说明 |
|---|---------|------|
| F1 | 时区 API 获取失败时"选择时区"占位的交互 | PRD 提到显示占位，但未说是否允许不选时区就进下一步 |
| F2 | 云账号绑定服务不可用（云端维护） | 绑定按钮点击后无响应/报错的降级提示 |

## 9. UI/交互一致性（Figma 对照） `[策略补充-UI]`

| # | 差异项 | PRD 描述 | Figma 实现 | 建议 |
|---|--------|---------|-----------|------|
| U1 | 绑定按钮文案 | "绑定云账号" | "去绑定" | 需确认以哪个为准 |
| U2 | 用户改进计划交互形式 | 页面内勾选框 | 独立弹窗 | **交互差异大**，需产品确认 |
| U3 | 第二步页面标题 | "注册管理员账号" | "注册设备" | 需确认 |
| U4 | 密码强度展示方式 | "低/中/高" 文字 | 4 条英文规则圆点指示器 | 需确认 |
| U5 | 协议文案措辞 | "《用户协议》和《隐私协议》" | "用户协议和隐私政策" | 需统一 |
| U6 | 弹窗页面标题 | （未定义） | "绑定邮箱" | 功能不匹配，疑似设计遗留 |
| U7 | 时区下拉选中后显示当前时间 | FR-1.4 明确要求 | Figma 未体现时间显示 | 需确认是否实现 |
